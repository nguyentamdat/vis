# plan-exit

## 1. Input

- No parameters (empty object).

## 2. Output

- `title`: `Switching to build agent`.
- `output`: Informational text for switching to build mode.
- `metadata`: Empty object.

## 3. JSON Example

```json
{}
```

## 4. Notes

- Internal tool id is `plan_exit`.
- Shows a confirmation prompt and creates a synthetic user message on approval.
